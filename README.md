To initialize and update the submodules in your repository, run the following commands:

```sh
git submodule init
```

```sh
git submodule update
```

This will ensure that all the submodules in your project are properly initialized and updated.
